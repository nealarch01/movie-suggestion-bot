// Call this function when using data obtained from tmdb
function attributeSource_tmdb(message: string): string {
    return `${message}Data obtained from themoviedb.org`;
}

export default attributeSource_tmdb;